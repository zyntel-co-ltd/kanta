# Kanta — Data Model

**Last updated:** 2026-03-28  

Document every significant table here: purpose, columns, types, constraints, RLS, business rules.

**Source of truth for DDL:** `supabase/migrations/` and `prisma/schema.prisma` (if used for types).

Feature blocks in `features/*.md` should link to table names defined here as this file is filled in.

*Initial stub — expand during schema work sessions.*
