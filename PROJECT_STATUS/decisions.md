# Kanta — Key Decisions

**Append-only. Never edit existing rows.**

| Decision | What was decided | Why | Date |
|----------|------------------|-----|------|
| Lab-first launch | Laboratory as first department | Radiology/pharmacy later | 2026-03 |
| Repo structure | Standalone Next.js repo `kanta` | — | — |
| Env var handling | Sanitize quotes in `lib/supabase.ts`, `lib/redis.ts` | Vercel copy/paste | 2026-03 |
| User provisioning | No public signup; admins add users | Security + tenant control | 2026-03 |
| Default after login | `/dashboard/home` | 3-app hub | 2026-03 |
| Navigation | Sidebar + app routing (see `Sidebar.tsx`) | UX | 2026-03 |
| Font | Inter via `next/font/google` | Brand readability | 2026-03 |
| ENG-154 (2026-04-01) | RLS enabled on `login_audit`, `qc_results`, `platform_admins`, `facility_invites`, `lab_sections`, `lab_shifts`. Function `search_path` hardened on `audit_trigger_fn`, `update_rack_status`, `custom_access_token_hook`, `update_updated_at`. `facility_invites.token` revoked for anon/authenticated SELECT. Leaked password protection to be enabled in Supabase Auth dashboard. | Close Supabase linter ERRORs before new hospital onboarding | 2026-04 |
| ENG-81 (2026-04-01) | Module colors locked as brand identifiers. Per-module theme alternatives cancelled. Current palette is permanent: Home/Admin/AI=Emerald `#059669`, Lab Metrics=Navy `#21336a`, Quality/Samples=Sky `#0284c7`, Assets=Slate `#475569`. | Recognition anchors for hospital staff; no theme switcher | 2026-04 |
| ENG-156 (2026-04-01) | **Zyntel Console** at `/dashboard/console` — super-admin-only cross-facility surface (sidebar + TopBar entry). Facilities list + provision facility admin; `POST /api/admin/users/sync` links orphaned `auth.users` into `facility_users` as viewer for the selected facility. | Platform ops; fixes Supabase-dashboard-created users invisible in Admin user list | 2026-04 |
| ENG-157 (2026-04-01) | **Pre-seed provisioning:** Zyntel provisions hospitals and first `facility_admin` via Console (`POST /api/console/facilities`, `POST /api/console/users`); credential handoff in UI. **Seed / self-serve** model (public signup, hospital configures itself) is out of scope for this decision — separate issue. Super-admin promotion remains SQL-only (`platform_admins` insert) with read-only list in Console. | Replace manual SQL onboarding; safe pre-seed ops; defer self-serve and super-admin UI | 2026-04 |
| ENG-104 (2026-04-01) | **Admin user management** — Table columns: display name (metadata chain, never user id), email, role labels from `FACILITY_ROLE_LABELS`, status, joined. Role edits respect hierarchy (`roleRank`); actors cannot change users above their rank (super-admin exempt). Deactivate only (`is_active`), no user delete API. Invites: `/api/invites` + pending resend/cancel. | Production-safe RBAC UX; aligns with ENG-105 admin panel gate | 2026-04 |
| ENG-160 (2026-04-01) | **Admin Panel is facility-scoped only** (ENG-155): user management, hospital identity fields, lab config, LIMS data connections, cancellations, audit — one facility context. **Zyntel Console** holds cross-facility operations (hospital/branch/tier provisioning). **Subscription tier** is read-only in Admin Hospital Settings; Zyntel changes tier in Console/ops. **Unmatched tests** tab/API: super-admin or deployment flag (`show-unmatched-tests` / env). **ENG-107 (re-scoped):** branch **creation** and cross-branch admin are **Console-only** (ENG-157); Admin **Hospital Settings** shows **current branch** + **read-only sibling branches**; cross-branch RBAC deferred to ENG-91. | Prevent Console concerns from bleeding into Admin; clear pre-seed vs in-app boundaries | 2026-04 |
| ENG-158 (2026-04-02) | **Console feature flags** — Super-admins manage PostHog per-facility overrides at `/dashboard/console/flags` using the PostHog REST API (`POSTHOG_PERSONAL_API_KEY` + `POSTHOG_PROJECT_ID`, server-only). Branch group overrides use `$group_key` = `facility_id`. **Reset to defaults** applies `getDefaultEnabledFlagsForTier` from `hospitals.tier` (free = all off; pro/enterprise = same module set until enterprise-only flags exist). Canonical keys live in `lib/featureFlagCatalog.ts`. | Remove Zyntel-only dependency on the PostHog dashboard for routine facility toggles | 2026-04 |
