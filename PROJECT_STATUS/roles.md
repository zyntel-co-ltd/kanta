# Kanta — User Roles & Permissions

**Last updated:** 2026-03-28  

RBAC is facility-scoped via `facility_users` (RBAC v2). Legacy role strings are normalized in `lib/auth/server.ts` (`normalizeFacilityRole`).

| Role | Who | Can | Cannot |
|------|-----|-----|--------|
| *(document per deployment)* | | | |

*Expand with exact role names and permissions from product + `facility_users` schema.*
